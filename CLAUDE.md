# CLAUDE.md

See [AGENTS.md](AGENTS.md) — the agent-facing contract for contributing to the
VibeGuard codebase (run `make ci` before every push, don't hand-edit generated
files, wire new rules into two import sites, keep the runtime-dependency budget,
SHA-pin actions). AGENTS.md is the single source of truth; this file is only a
pointer so Claude-based tools find it by name.
