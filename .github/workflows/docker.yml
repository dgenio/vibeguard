name: Docker

on:
  push:
    branches: [main, master]
    paths:
      - "Dockerfile"
      - ".dockerignore"
      - "pyproject.toml"
      - "vibeguard/**"
      - ".github/workflows/docker.yml"
  pull_request:
    paths:
      - "Dockerfile"
      - ".dockerignore"
      - "pyproject.toml"
      - "vibeguard/**"
      - ".github/workflows/docker.yml"

permissions:
  contents: read

jobs:
  build:
    name: Build & smoke-test image
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@09e4493aee4ae00cbdca5871a56c88a35d27d859 # v4

      - name: Build image
        uses: docker/build-push-action@4b7708d7f42032839359c64671e7eef7f09192da # v7
        with:
          context: .
          push: false
          load: true
          tags: vibeguard:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Smoke test — version
        run: docker run --rm vibeguard:ci version

      - name: Smoke test — scan vulnerable-node-package (JSON)
        run: |
          docker run --rm \
            -v "${{ github.workspace }}/examples:/scan:ro" \
            vibeguard:ci scan \
              --path /scan/vulnerable-node-package \
              --json \
            > /tmp/findings.json
          test -s /tmp/findings.json
          jq -e '.findings | length > 0' /tmp/findings.json

      - name: Smoke test — non-root user
        run: |
          UID_RUN=$(docker run --rm --entrypoint id vibeguard:ci -u)
          test "$UID_RUN" -ne 0
